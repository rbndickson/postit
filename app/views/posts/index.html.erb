<%= render 'shared/content_title', title: 'All Posts' %>

<!-- <% @posts.each do |post| %>
  <%= render 'post', post: post %>
<% end %> -->

<div id="my-posts">
  <%= render @posts %>
</div>

<div id="infinite-scrolling">
  <%= will_paginate @posts%>
</div>
